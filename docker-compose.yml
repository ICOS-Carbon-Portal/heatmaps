services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - heatmaps-cache:/app/cache
      - heatmaps-output:/app/output
    environment:
      - PORT=5000
    restart: unless-stopped

volumes:
  heatmaps-cache:
  heatmaps-output:
